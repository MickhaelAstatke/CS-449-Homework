import React from 'react'
import ReactDOM from 'react-dom/client'
import Sprint0GUI from './Sprint0GUI'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Sprint0GUI />
  </React.StrictMode>,
)